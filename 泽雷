名称：安卓 CI

在……之上:
  推:
分支机构：["main"]
  pull_request:
分支机构：["main"]

工作:
  建立:
    跑步次数-on:ubuntu-latest
权限：# 👈 显式授权
      contents: write

    步骤:
-使用：操作/结帐@v4

-名称：设置JDK17
用法：操作/设置-爪哇@V4
        和……一起:
          爪哇-版本：'17'
分布：'temurin'
          cache: gradle

-名称：授予gradlew执行权限
运行：chmod+x gradlew

      - name: Build Release APK
assembleRelease

      #只在推时发版；公关时跳过
      - name: Release
        if: github.event_name == 'push'
用途：软作物/行动-生长激素-释放@V2
        和……一起:
标记名称(_N)：V1.0.${{ github.run_number }}
名称：版本v1.0.${{github.run_number}}
文件：应用程序/建立/输出/安装包/释放/应用程序-释放.安装包
生成发布注释(_R)：正确
draft: false
预发行：假的
        env:
          GITHUB_TOKEN: ${{ 秘密.github_TOKEN}}运行：./gradlew
